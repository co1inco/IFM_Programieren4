{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Demo: exploded",
			"type": "shell",
			"command": "mvn resources:resources compiler:compile war:exploded",
			"group": "build",
			"options": {
				"cwd": "${workspaceFolder}"
			}
		},
		{
			"label": "Demo: war",
			"type": "shell",
			"command": "mvn resources:resources compiler:compile war:war",
			"group": "build",
			"options": {
				"cwd": "${workspaceFolder}"
			}
		}
	]
}